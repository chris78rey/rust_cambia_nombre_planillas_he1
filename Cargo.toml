[package]
name = "rust_cambia_nombre_planillas_he1"
version = "0.1.0"
edition = "2024"

[dependencies]
lopdf = "0.38"
dotenvy = "0.15"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "multipart", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
